#!/usr/bin/env bash
set -euo pipefail

if ! command -v pnpm >/dev/null 2>&1; then
  echo "pnpm 11.23.0 is required. Install the pinned packageManager version first." >&2
  exit 127
fi

exec pnpm exec tsx tools/site.ts "$@"
