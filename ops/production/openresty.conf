worker_processes auto;
pid /tmp/openresty.pid;
error_log /dev/stderr notice;

events {
  worker_connections 1024;
}

http {
  log_format structured escape=json '{'
    '"timestamp":"$time_iso8601",'
    '"level":"info",'
    '"component":"openresty",'
    '"event":"http_request_completed",'
    '"request_id":"$request_id",'
    '"host":"$host",'
    '"method":"$request_method",'
    '"route":"$uri",'
    '"status":$status,'
    '"duration_seconds":$request_time,'
    '"upstream_status":"$upstream_status",'
    '"upstream_duration_seconds":"$upstream_response_time",'
    '"request_bytes":$request_length,'
    '"response_bytes":$bytes_sent'
  '}';
  access_log /dev/stdout structured;
  limit_req_zone $binary_remote_addr zone=public_origin:10m rate=50r/s;
  limit_req_zone $binary_remote_addr zone=control_origin:10m rate=20r/s;
  limit_req_status 429;
  client_body_temp_path /tmp/client-body;
  fastcgi_temp_path /tmp/fastcgi;
  proxy_temp_path /tmp/proxy;
  scgi_temp_path /tmp/scgi;
  uwsgi_temp_path /tmp/uwsgi;
  resolver 127.0.0.11 valid=5s ipv6=off;
  set_real_ip_from 127.0.0.1;
  set_real_ip_from 172.16.0.0/12;
  real_ip_header X-Forwarded-For;
  real_ip_recursive on;

  server {
    listen 8082;
    listen [::]:8082;
    server_name www.tungchiahui.cn ddns.tungchiahui.cn;

    add_header Content-Security-Policy "default-src 'self'; base-uri 'self'; connect-src 'self' https://umami.tungchiahui.cn; font-src 'self' data: https://cdn.tungchiahui.cn https://global.cdn.tungchiahui.cn; form-action 'self'; frame-src https://umami.tungchiahui.cn; frame-ancestors 'none'; img-src 'self' data: https:; media-src 'self' https:; object-src 'none'; script-src 'self' 'unsafe-inline' https://umami.tungchiahui.cn https://cdn.tungchiahui.cn https://global.cdn.tungchiahui.cn; style-src 'self' 'unsafe-inline' https://cdn.tungchiahui.cn https://global.cdn.tungchiahui.cn; upgrade-insecure-requests" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Permissions-Policy "camera=(), geolocation=(), microphone=(), payment=(), usb=()" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;

    location = /openresty-health {
      access_log off;
      default_type application/json;
      return 200 '{"service":"openresty","status":"ok"}';
    }

    location ^~ /api/internal/ {
      return 404;
    }

    location ^~ /api/ops/ {
      if ($request_method !~ ^(GET|POST|PUT)$) { return 405; }
      limit_req zone=control_origin burst=40 nodelay;
      set $control_upstream control-api:8080;
      proxy_pass http://$control_upstream;
      proxy_http_version 1.1;
      proxy_connect_timeout 2s;
      proxy_read_timeout 30s;
      proxy_send_timeout 5s;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Request-ID $request_id;
      proxy_cache_bypass 1;
      proxy_no_cache 1;
      proxy_hide_header Cache-Control;
      add_header Cache-Control "no-store" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header Content-Security-Policy "default-src 'none'; frame-ancestors 'none'" always;
      add_header Permissions-Policy "camera=(), geolocation=(), microphone=(), payment=(), usb=()" always;
      add_header Referrer-Policy "no-referrer" always;
      add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
      add_header X-Frame-Options "DENY" always;
    }

    location = /api/traffic {
      if ($request_method != POST) { return 405; }
      limit_req zone=public_origin burst=100 nodelay;
      include /etc/tungchiahui/deployment/active-slot.conf;
      proxy_pass http://$web_upstream;
      proxy_hide_header Content-Security-Policy;
      proxy_http_version 1.1;
      proxy_connect_timeout 2s;
      proxy_read_timeout 30s;
      proxy_send_timeout 5s;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Request-ID $request_id;
    }

    location / {
      if ($request_method !~ ^(GET|HEAD)$) { return 405; }
      limit_req zone=public_origin burst=100 nodelay;
      include /etc/tungchiahui/deployment/active-slot.conf;
      proxy_pass http://$web_upstream;
      proxy_hide_header Content-Security-Policy;
      proxy_http_version 1.1;
      proxy_connect_timeout 2s;
      proxy_read_timeout 30s;
      proxy_send_timeout 5s;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Request-ID $request_id;
    }
  }
}
