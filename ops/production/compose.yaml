name: tungchiahui-production

x-hardened-service: &hardened-service
  restart: unless-stopped
  read_only: true
  cap_drop: [ALL]
  security_opt:
    - no-new-privileges:true

x-node-tmpfs: &node-tmpfs
  - /tmp:size=32m,mode=1777,noexec,nosuid,nodev

x-web-tmpfs: &web-tmpfs
  - /tmp:size=32m,mode=1777,noexec,nosuid,nodev
  - /app/.next/cache:size=64m,mode=0755,uid=10001,gid=10001,nosuid,nodev

services:
  postgres:
    image: ${TUNGCHIAHUI_POSTGRES_IMAGE:?immutable PostgreSQL image is required}
    restart: unless-stopped
    read_only: true
    cap_drop: [ALL]
    security_opt:
      - no-new-privileges:true
    user: "70:70"
    group_add: ["10050"]
    environment:
      PGBACKREST_REPO1_CIPHER_PASS: ${PGBACKREST_REPO1_CIPHER_PASS:?pgBackRest cipher is required}
      POSTGRES_DB: ${POSTGRES_DB:?PostgreSQL database is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?PostgreSQL password is required}
      POSTGRES_USER: ${POSTGRES_USER:?PostgreSQL user is required}
    command:
      - postgres
      - -c
      - archive_mode=on
      - -c
      - archive_command=pgbackrest --config=/etc/pgbackrest/pgbackrest.conf --stanza=tungchiahui archive-push %p
      - -c
      - archive_timeout=60
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [database]
    tmpfs:
      - /tmp:size=32m,mode=1777,noexec,nosuid,nodev
    volumes:
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/postgres
        target: /var/lib/postgresql
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/backup-repository
        target: /var/lib/pgbackrest
      - ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
      - postgres-socket:/run/postgresql

  pgbouncer:
    <<: *hardened-service
    image: percona/percona-pgbouncer:1.25.2-5@sha256:ee8f9b3e8b80b379b47ae41419a0d16de7a20c2be0cae5dbf55fe403d3d9f33d
    user: "1001:10050"
    entrypoint: [/usr/bin/pgbouncer]
    command: [/etc/pgbouncer/pgbouncer.ini]
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: [CMD-SHELL, "timeout 2 bash -c '</dev/tcp/127.0.0.1/6432'"]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [application, database]
    tmpfs: *node-tmpfs
    volumes:
      - ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/pgbouncer.ini:/etc/pgbouncer/pgbouncer.ini:ro
      - ${TUNGCHIAHUI_SECRET_DIRECTORY:?secret directory is required}/pgbouncer-userlist.txt:/etc/pgbouncer/userlist.txt:ro

  database-role-bootstrap:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_SERVICE_IMAGE:?immutable service image is required}
    restart: "no"
    profiles: [provision]
    user: "10006:10050"
    command: [node, dist/database-role-bootstrap.cjs]
    environment:
      DATABASE_ADMIN_URL: ${DATABASE_ADMIN_URL:?database bootstrap admin URL is required}
      SITE_APP_LOGIN_NAME: ${SITE_APP_LOGIN_NAME:?app login name is required}
      SITE_APP_LOGIN_PASSWORD: ${SITE_APP_LOGIN_PASSWORD:?app login password is required}
      SITE_CONTENT_WORKER_LOGIN_NAME: ${SITE_CONTENT_WORKER_LOGIN_NAME:?content worker login name is required}
      SITE_CONTENT_WORKER_LOGIN_PASSWORD: ${SITE_CONTENT_WORKER_LOGIN_PASSWORD:?content worker login password is required}
      SITE_CONTROL_API_LOGIN_NAME: ${SITE_CONTROL_API_LOGIN_NAME:?control-api login name is required}
      SITE_CONTROL_API_LOGIN_PASSWORD: ${SITE_CONTROL_API_LOGIN_PASSWORD:?control-api login password is required}
      SITE_MIGRATOR_LOGIN_NAME: ${SITE_MIGRATOR_LOGIN_NAME:?migrator login name is required}
      SITE_MIGRATOR_LOGIN_PASSWORD: ${SITE_MIGRATOR_LOGIN_PASSWORD:?migrator login password is required}
    volumes:
      - ${TUNGCHIAHUI_SECRET_DIRECTORY:?secret directory is required}/pgbouncer-userlist.txt:/run/secrets/pgbouncer-userlist.txt:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks: [database]
    tmpfs: *node-tmpfs

  database-migrate:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_SERVICE_IMAGE:?immutable service image is required}
    restart: "no"
    profiles: [deployment]
    user: "10006:10050"
    command: [node, dist/database-migrate.cjs]
    environment:
      DATABASE_URL: ${DATABASE_MIGRATE_URL:?database migration URL is required}
      DEPLOYMENT_HAS_FRESH_RECOVERABLE_BACKUP: "false"
      SITE_RUNTIME_MODE: production
    networks: [database]
    tmpfs: *node-tmpfs

  control-api:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_SERVICE_IMAGE:?immutable service image is required}
    user: "10002:10050"
    command: [node, dist/control-api.cjs]
    environment:
      CONTROL_API_HOST: 0.0.0.0
      CONTROL_API_PORT: "8080"
      CONTROL_GITHUB_OIDC_POLICY_JSON: ${CONTROL_GITHUB_OIDC_POLICY_JSON:?GitHub OIDC policy is required}
      CONTROL_OPERATOR_KEYS_JSON: ${CONTROL_OPERATOR_KEYS_JSON:?operator keys are required}
      CONTROL_RATE_LIMIT_PER_MINUTE: ${TUNGCHIAHUI_CONTROL_RATE_LIMIT_PER_MINUTE:-120}
      CONTROL_REPLAY_WINDOW_SECONDS: "300"
      CONTROL_STATE_PATH: /control-state/control.db
      DATABASE_URL: ${CONTROL_API_DATABASE_URL:?control-api database URL is required}
      OWNER_PASSWORD_HASH: ${OWNER_PASSWORD_HASH:-}
      SITE_RUNTIME_MODE: production
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:8080/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [edge, application]
    tmpfs: *node-tmpfs
    volumes:
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/control-state
        target: /control-state

  content-worker:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_SERVICE_IMAGE:?immutable service image is required}
    user: "10003:10050"
    command: [node, dist/content-worker.cjs]
    environment:
      CONTENT_WORKER_HOST: 0.0.0.0
      CONTENT_WORKER_POLLING_ENABLED: ${TUNGCHIAHUI_CONTENT_POLLING_ENABLED:-false}
      CONTENT_WORKER_PORT: "8083"
      DATABASE_URL: ${CONTENT_WORKER_DATABASE_URL:?content-worker database URL is required}
      GITHUB_CONTENT_READ_TOKEN: ${GITHUB_CONTENT_READ_TOKEN:-}
      GITHUB_CONTENT_REPOSITORY: ${GITHUB_CONTENT_REPOSITORY:?content repository is required}
      SEARCH_WORKER_POLLING_ENABLED: ${TUNGCHIAHUI_SEARCH_POLLING_ENABLED:-false}
      SITE_REVALIDATION_ENDPOINT: http://openresty:8085/api/internal/revalidate
      SITE_REVALIDATION_SECRET: ${SITE_REVALIDATION_SECRET:?revalidation secret is required}
      SITE_RUNTIME_MODE: production
      TRANSLATION_PROVIDER: disabled
      TRANSLATION_WORKER_POLLING_ENABLED: "false"
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:8083/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [application, content-egress, deployment-probe]
    tmpfs: *node-tmpfs

  observability-agent:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_SERVICE_IMAGE:?immutable service image is required}
    user: "10007:10050"
    command: [node, dist/observability-agent.cjs]
    depends_on:
      content-worker:
        condition: service_healthy
      control-api:
        condition: service_healthy
      deploy-agent:
        condition: service_healthy
      openresty:
        condition: service_healthy
      pgbouncer:
        condition: service_healthy
    environment:
      OBSERVABILITY_ALERT_WEBHOOK_BEARER_TOKEN: ${OBSERVABILITY_ALERT_WEBHOOK_BEARER_TOKEN:-}
      OBSERVABILITY_ALERT_WEBHOOK_URL: ${OBSERVABILITY_ALERT_WEBHOOK_URL:-}
      OBSERVABILITY_BACKUP_MAX_AGE_SECONDS: ${TUNGCHIAHUI_OBSERVABILITY_BACKUP_MAX_AGE_SECONDS:?backup monitoring policy is required}
      OBSERVABILITY_CONTROL_URL: http://control-api:8080/health
      OBSERVABILITY_DEPLOY_AGENT_URL: http://deploy-agent:8081/health
      OBSERVABILITY_DISK_CRITICAL_PERCENT: ${TUNGCHIAHUI_OBSERVABILITY_DISK_CRITICAL_PERCENT:-90}
      OBSERVABILITY_HOST: 0.0.0.0
      OBSERVABILITY_INTERVAL_SECONDS: ${TUNGCHIAHUI_OBSERVABILITY_INTERVAL_SECONDS:-30}
      OBSERVABILITY_JOB_MAX_AGE_SECONDS: ${TUNGCHIAHUI_OBSERVABILITY_JOB_MAX_AGE_SECONDS:-900}
      OBSERVABILITY_LATENCY_WARNING_MS: ${TUNGCHIAHUI_OBSERVABILITY_LATENCY_WARNING_MS:-2000}
      OBSERVABILITY_ORIGIN_HOSTNAME: ${TUNGCHIAHUI_OBSERVABILITY_ORIGIN_HOSTNAME:-ddns.tungchiahui.cn}
      OBSERVABILITY_ORIGIN_IPV6_REQUIRED: ${TUNGCHIAHUI_OBSERVABILITY_ORIGIN_IPV6_REQUIRED:-true}
      OBSERVABILITY_ORIGIN_SERVER_NAME: ${TUNGCHIAHUI_OBSERVABILITY_ORIGIN_SERVER_NAME:-ddns.tungchiahui.cn}
      OBSERVABILITY_ORIGIN_URL: ${TUNGCHIAHUI_OBSERVABILITY_ORIGIN_URL:-https://ddns.tungchiahui.cn:8443/api/ready}
      OBSERVABILITY_PGBOUNCER_HOST: pgbouncer
      OBSERVABILITY_PGBOUNCER_PORT: "6432"
      OBSERVABILITY_PORT: "8084"
      OBSERVABILITY_PUBLIC_ASSET_PATH: ${TUNGCHIAHUI_OBSERVABILITY_PUBLIC_ASSET_PATH:-/api/assets/monitoring/health.svg}
      OBSERVABILITY_PUBLIC_SERVER_NAME: ${TUNGCHIAHUI_OBSERVABILITY_PUBLIC_SERVER_NAME:-www.tungchiahui.cn}
      OBSERVABILITY_PUBLIC_URL: ${TUNGCHIAHUI_OBSERVABILITY_PUBLIC_URL:-https://www.tungchiahui.cn/}
      OBSERVABILITY_RESTORE_DRILL_MAX_AGE_SECONDS: ${TUNGCHIAHUI_OBSERVABILITY_RESTORE_DRILL_MAX_AGE_SECONDS:?restore-drill policy is required}
      OBSERVABILITY_RESTORE_DRILL_TIMESTAMP: ${TUNGCHIAHUI_OBSERVABILITY_RESTORE_DRILL_TIMESTAMP:?last restore-drill evidence is required}
      OBSERVABILITY_WEB_URL: http://openresty:8082/api/ready
      OBSERVABILITY_WORKER_URL: http://content-worker:8083/health
      SITE_RUNTIME_MODE: production
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:8084/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 3s
      retries: 30
    networks: [application, deployment-probe, edge]
    tmpfs: *node-tmpfs
    volumes:
      - ${TUNGCHIAHUI_DATA_ROOT:?data root is required}:/host-data:ro
      - ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/control-state:/host-control-state:ro

  web-blue:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_WEB_BLUE_IMAGE:?immutable blue web image is required}
    user: "10001:10001"
    environment:
      ASSET_S3_ACCESS_KEY_ID: ${ASSET_S3_ACCESS_KEY_ID:?asset S3 access key is required}
      ASSET_S3_BUCKET: ${ASSET_S3_BUCKET:?asset S3 bucket is required}
      ASSET_S3_ENDPOINT: ${ASSET_S3_ENDPOINT:?asset S3 endpoint is required}
      ASSET_S3_FORCE_PATH_STYLE: ${ASSET_S3_FORCE_PATH_STYLE:-true}
      ASSET_S3_REGION: ${ASSET_S3_REGION:-us-east-1}
      ASSET_S3_SECRET_ACCESS_KEY: ${ASSET_S3_SECRET_ACCESS_KEY:?asset S3 secret is required}
      DATABASE_URL: ${WEB_DATABASE_URL:?web database URL is required}
      SITE_BASE_URL: ${SITE_BASE_URL:?site base URL is required}
      SITE_DEPLOYMENT_SHA: ${TUNGCHIAHUI_BLUE_DEPLOYMENT_SHA:?blue Git SHA is required}
      SITE_REVALIDATION_SECRET: ${SITE_REVALIDATION_SECRET:?revalidation secret is required}
      SITE_RUNTIME_MODE: production
      SITE_SLOT: blue
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:3000/api/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 45
    networks: [edge, application, deployment-probe]
    tmpfs: *web-tmpfs

  web-green:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_WEB_GREEN_IMAGE:?immutable green web image is required}
    user: "10001:10001"
    environment:
      ASSET_S3_ACCESS_KEY_ID: ${ASSET_S3_ACCESS_KEY_ID:?asset S3 access key is required}
      ASSET_S3_BUCKET: ${ASSET_S3_BUCKET:?asset S3 bucket is required}
      ASSET_S3_ENDPOINT: ${ASSET_S3_ENDPOINT:?asset S3 endpoint is required}
      ASSET_S3_FORCE_PATH_STYLE: ${ASSET_S3_FORCE_PATH_STYLE:-true}
      ASSET_S3_REGION: ${ASSET_S3_REGION:-us-east-1}
      ASSET_S3_SECRET_ACCESS_KEY: ${ASSET_S3_SECRET_ACCESS_KEY:?asset S3 secret is required}
      DATABASE_URL: ${WEB_DATABASE_URL:?web database URL is required}
      SITE_BASE_URL: ${SITE_BASE_URL:?site base URL is required}
      SITE_DEPLOYMENT_SHA: ${TUNGCHIAHUI_GREEN_DEPLOYMENT_SHA:?green Git SHA is required}
      SITE_REVALIDATION_SECRET: ${SITE_REVALIDATION_SECRET:?revalidation secret is required}
      SITE_RUNTIME_MODE: production
      SITE_SLOT: green
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:3000/api/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 45
    networks: [edge, application, deployment-probe]
    tmpfs: *web-tmpfs

  deploy-agent:
    <<: *hardened-service
    image: ${TUNGCHIAHUI_RECOVERY_IMAGE:?immutable recovery image is required}
    user: "70:10050"
    group_add:
      - ${TUNGCHIAHUI_DOCKER_SOCKET_GID:?Docker socket GID is required}
    command: [node, dist/deploy-agent.cjs]
    depends_on:
      control-api:
        condition: service_healthy
    environment:
      ASSET_S3_ACCESS_KEY_ID: ${ASSET_S3_ACCESS_KEY_ID:?asset S3 access key is required}
      ASSET_S3_BUCKET: ${ASSET_S3_BUCKET:?asset S3 bucket is required}
      ASSET_S3_ENDPOINT: ${ASSET_S3_ENDPOINT:?asset S3 endpoint is required}
      ASSET_S3_FORCE_PATH_STYLE: ${ASSET_S3_FORCE_PATH_STYLE:-true}
      ASSET_S3_REGION: ${ASSET_S3_REGION:-us-east-1}
      ASSET_S3_SECRET_ACCESS_KEY: ${ASSET_S3_SECRET_ACCESS_KEY:?asset S3 secret is required}
      BACKUP_AGE_IDENTITY_PATH: /run/secrets/backup-age-identity.txt
      BACKUP_AGE_RECIPIENT: ${BACKUP_AGE_RECIPIENT:?backup age recipient is required}
      BACKUP_LOCAL_REPOSITORY_PATH: /var/lib/pgbackrest
      BACKUP_OFFSITE_S3_ACCESS_KEY_ID: ${BACKUP_OFFSITE_S3_ACCESS_KEY_ID:?off-site backup access key is required}
      BACKUP_OFFSITE_S3_BUCKET: ${BACKUP_OFFSITE_S3_BUCKET:?off-site backup bucket is required}
      BACKUP_OFFSITE_S3_ENDPOINT: ${BACKUP_OFFSITE_S3_ENDPOINT:?off-site backup endpoint is required}
      BACKUP_OFFSITE_S3_FORCE_PATH_STYLE: ${BACKUP_OFFSITE_S3_FORCE_PATH_STYLE:-false}
      BACKUP_OFFSITE_S3_REGION: ${BACKUP_OFFSITE_S3_REGION:-auto}
      BACKUP_OFFSITE_S3_SECRET_ACCESS_KEY: ${BACKUP_OFFSITE_S3_SECRET_ACCESS_KEY:?off-site backup secret is required}
      BACKUP_PGBACKREST_CONFIG_PATH: /etc/pgbackrest/pgbackrest.conf
      BACKUP_PGBACKREST_STANZA: tungchiahui
      BACKUP_POSTGRES_DATA_PATH: /var/lib/postgresql/18/docker
      BACKUP_REPLICATION_CONCURRENCY: ${TUNGCHIAHUI_BACKUP_REPLICATION_CONCURRENCY:-8}
      BACKUP_S3_ACCESS_KEY_ID: ${BACKUP_S3_ACCESS_KEY_ID:?backup access key is required}
      BACKUP_S3_BUCKET: ${BACKUP_S3_BUCKET:?backup bucket is required}
      BACKUP_S3_ENDPOINT: ${BACKUP_S3_ENDPOINT:?backup endpoint is required}
      BACKUP_S3_FORCE_PATH_STYLE: ${BACKUP_S3_FORCE_PATH_STYLE:-true}
      BACKUP_S3_REGION: ${BACKUP_S3_REGION:-us-east-1}
      BACKUP_S3_SECRET_ACCESS_KEY: ${BACKUP_S3_SECRET_ACCESS_KEY:?backup secret is required}
      BACKUP_WORK_DIRECTORY: /var/lib/tungchiahui-backup-work
      CONTROL_STATE_PATH: /control-state/control.db
      DEPLOYMENT_ACTIVE_SLOT_CONFIG_PATH: /deployment-config/active-slot.conf
      DEPLOYMENT_ARTICLE_PATH: ${TUNGCHIAHUI_DEPLOYMENT_ARTICLE_PATH:?representative article path is required}
      DEPLOYMENT_ASSET_PATH: ${TUNGCHIAHUI_DEPLOYMENT_ASSET_PATH:?representative asset path is required}
      DEPLOYMENT_BACKUP_MAX_AGE_SECONDS: ${TUNGCHIAHUI_DEPLOYMENT_BACKUP_MAX_AGE_SECONDS:?backup freshness policy is required}
      DEPLOYMENT_BLUE_CONTAINER_NAME: ${TUNGCHIAHUI_BLUE_CONTAINER_NAME:?blue container name is required}
      DEPLOYMENT_BLUE_URL: http://web-blue:3000
      DEPLOYMENT_GREEN_CONTAINER_NAME: ${TUNGCHIAHUI_GREEN_CONTAINER_NAME:?green container name is required}
      DEPLOYMENT_GREEN_URL: http://web-green:3000
      DEPLOYMENT_IMAGE_REPOSITORY: ${TUNGCHIAHUI_DEPLOYMENT_IMAGE_REPOSITORY:?approved image repository is required}
      DEPLOYMENT_JOURNAL_PATH: /app/deployment/drizzle/meta/_journal.json
      DEPLOYMENT_MIGRATION_CONTAINER_NAME: ${TUNGCHIAHUI_MIGRATION_CONTAINER_NAME:?migration container name is required}
      DEPLOYMENT_MIGRATION_POLICY_PATH: /app/deployment/drizzle/migration-policy.json
      DEPLOYMENT_OPENRESTY_CONTAINER_NAME: ${TUNGCHIAHUI_OPENRESTY_CONTAINER_NAME:?OpenResty container name is required}
      DEPLOYMENT_POLLING_ENABLED: "true"
      DEPLOYMENT_PUBLIC_ENTRY_URL: http://openresty:8082
      DEPLOYMENT_REGISTRY_TOKEN: ${DEPLOYMENT_REGISTRY_TOKEN:?deployment registry token is required}
      DEPLOYMENT_REGISTRY_USERNAME: ${DEPLOYMENT_REGISTRY_USERNAME:?deployment registry username is required}
      DEPLOYMENT_SEARCH_QUERY: ${TUNGCHIAHUI_DEPLOYMENT_SEARCH_QUERY:?representative search query is required}
      DEPLOY_AGENT_HOST: 0.0.0.0
      DEPLOY_AGENT_PORT: "8081"
      DOCKER_SOCKET_PATH: /run/deploy-capability/docker.sock
      POSTGRES_CONTAINER_NAME: ${TUNGCHIAHUI_POSTGRES_CONTAINER_NAME:?PostgreSQL container name is required}
      PGBACKREST_REPO1_CIPHER_PASS: ${PGBACKREST_REPO1_CIPHER_PASS:?pgBackRest cipher is required}
      RECOVERY_POLLING_ENABLED: "true"
      SITE_BASE_URL: ${SITE_BASE_URL:?site base URL is required}
      SITE_REVALIDATION_SECRET: ${SITE_REVALIDATION_SECRET:?revalidation secret is required}
      SITE_RUNTIME_MODE: production
      WEB_DATABASE_URL: ${WEB_DATABASE_URL:?web database URL is required}
    healthcheck:
      test: [CMD, node, -e, "fetch('http://127.0.0.1:8081/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [backup-egress, deploy-control, deployment-probe]
    tmpfs: *node-tmpfs
    volumes:
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/control-state
        target: /control-state
      - type: bind
        source: ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/deployment
        target: /deployment-config
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/postgres
        target: /var/lib/postgresql
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/backup-repository
        target: /var/lib/pgbackrest
      - type: bind
        source: ${TUNGCHIAHUI_DATA_ROOT:?data root is required}/backup-work
        target: /var/lib/tungchiahui-backup-work
      - ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
      - ${TUNGCHIAHUI_SECRET_DIRECTORY:?secret directory is required}/backup-age-identity.txt:/run/secrets/backup-age-identity.txt:ro
      - postgres-socket:/run/postgresql
      - /var/run/docker.sock:/run/deploy-capability/docker.sock:ro

  openresty:
    <<: *hardened-service
    # The loopback ingress must return after a Docker daemon restart even if the
    # daemon previously persisted a stopped state for this container.
    restart: always
    image: openresty/openresty:1.31.1.1-2-alpine-fat@sha256:427d94fea0c24b099e7891e8d1b7976f6d008e2d427e56bab725c8b8b293795b
    user: "10005:10050"
    depends_on:
      control-api:
        condition: service_healthy
    healthcheck:
      test: [CMD, curl, --fail, --silent, http://127.0.0.1:8082/openresty-health]
      interval: 5s
      timeout: 3s
      retries: 30
    networks: [edge, deployment-probe]
    ports:
      - target: 8082
        published: "${TUNGCHIAHUI_ORIGIN_PORT:-3100}"
        host_ip: "${TUNGCHIAHUI_ORIGIN_BIND_ADDRESS:-127.0.0.1}"
        protocol: tcp
    tmpfs:
      - /tmp:size=32m,mode=1777,noexec,nosuid,nodev
      - /var/run:size=8m,mode=755,nosuid,nodev
    volumes:
      - ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/openresty.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro
      - ${TUNGCHIAHUI_CONFIG_ROOT:?config root is required}/deployment:/etc/tungchiahui/deployment:ro

networks:
  application:
    internal: true
  database:
    internal: true
  deploy-control:
    internal: true
  deployment-probe:
    internal: true
  backup-egress:
  content-egress: {}
  edge:
    enable_ipv6: true

volumes:
  postgres-socket:
