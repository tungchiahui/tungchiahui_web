// PostCSS loads its configuration as a native module; application and automation source remains TypeScript.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
